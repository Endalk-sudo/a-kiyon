rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All data access is through server-side Firebase Admin SDK.
    // Direct client access is not used — block everything.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
