# pnpm 10+ reads dependency overrides from this file (the package.json
# `overrides` key is silently ignored). Used to force patched versions of
# transitive deps flagged by `pnpm audit`.
overrides:
  brace-expansion: ^2.1.4
  uuid: ^11.1.1
